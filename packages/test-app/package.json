{
  "name": "test-app",
  "version": "1.0.34",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist/ build/",
    "build": "tsc -b && rollup -c",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "execa": "^7.1.1",
    "fs-extra": "^11.1.1",
    "rollup-plugin-in-it-stats": "1.0.34",
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "rollup": "^3.20.2"
  }
}
