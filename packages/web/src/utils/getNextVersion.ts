// import { StatsDocument } from 'in-it-shared-types';

// export async function getNextVersion({
//   appId,
//   envirmonet,
//   branch,
//   compilation,
// }: Pick<StatsDocument, 'appId' | 'envirmonet' | 'branch' | 'compilation'>): Promise<string> {
//   // query latest version from db and increment it
//   // if not found, set to pre-alpha 0.0.0
//   return 'pre-alpha 0.0.0';
// }
